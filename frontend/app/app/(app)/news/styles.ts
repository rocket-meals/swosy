import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  newsContainer: {
    flex: 1,
  },
  newsContentContainer: {
    width: '100%',
    alignItems: 'center',
    paddingVertical: 20,
  },
  newsListContainer: {
    width: '100%',
    gap: 20,
  },
});
