This File is just the project root file, so that sub processes can find the project root and use it as a reference point for relative paths.

Do not delete or move this file, as it is used by the project to find the root directory.