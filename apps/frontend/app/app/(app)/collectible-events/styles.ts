import { StyleSheet } from 'react-native';

export default StyleSheet.create({
        container: {
                width: '100%',
                padding: 20,
        },
        emptyText: {
                textAlign: 'center',
                marginTop: 20,
                fontSize: 16,
        },
});
