/**
 * Route of the housing analytics screens.
 *
 * Kept here rather than in `AppScreens` (repo-depkit-common) because these screens are Rocket
 * Meals management tooling, not a deep-linkable app section shared with the other apps - the
 * chats screens are handled the same way.
 */
export const HOUSING_ANALYTICS_ROUTE = '/housing-analytics';
