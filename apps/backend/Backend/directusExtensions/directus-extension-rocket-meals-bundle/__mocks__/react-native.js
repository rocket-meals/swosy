// Minimal mock for react-native in Jest (Node environment).
// Only type-only imports from react-native are used in the frontend config,
// so no actual exports are needed at runtime.
module.exports = {};
