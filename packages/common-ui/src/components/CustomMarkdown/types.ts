export interface CustomMarkdownProps {
	content: string;
	/** Accent color for headings/collapsible sections/buttons. Defaults to the theme's primary color. */
	backgroundColor?: string;
	imageHeight?: string | number;
	imageWidth?: string | number;
}
